# Core dependencies (cross-platform)
# Note: Install torch first before running pip install -r requirements.txt
accelerate>=1.1.1
diffusers>=0.30.0
einops>=0.8.1
huggingface_hub>=0.36.0
imageio>=2.36.0
omegaconf>=2.3.0
opencv-python-headless>=4.10.0
Pillow>=11.0.0
pymeshlab
pytorch_lightning>=1.9.5
PyYAML>=6.0.2
safetensors>=0.4.5
scikit-image>=0.24.0
onnxruntime
rembg
timm>=1.0.11
torchdiffeq>=0.2.5
tqdm>=4.66.5
transformers>=4.37.2
trimesh>=4.4.7
gradio>=4.0.0

# =============================================================================
# OPTIONAL: CUDA-only packages (not needed for Mac/MPS)
# =============================================================================
# These packages provide GPU acceleration on NVIDIA GPUs but are not
# required for running on Mac (MPS) or CPU.
#
# For NVIDIA GPU users:
#   pip install diso==0.1.4  # differentiable marching cubes
#   pip install flash_attn==2.8.3
#   pip install sageattention==1.0.6
#   pip install deepspeed
#   pip install git+https://github.com/ashawkey/cubvh --no-build-isolation
#   pip install https://data.pyg.org/whl/torch-2.5.0%2Bcu121/torch_cluster-1.6.3%2Bpt25cu121-cp310-cp310-linux_x86_64.whl
# =============================================================================
